<%= form_with(model: @spot_register_form, url: url, method: method, data: { turbo: false }) do |f| %>
  <input type="button" onclick="submit();" value=<%= btn_display %> class="confirm-btn button" />
<% end %>
