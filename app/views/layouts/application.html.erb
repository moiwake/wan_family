<!DOCTYPE html>
<html>
  <head>
    <title>WanFamily[ワン・ファミリー] -ワンちゃん・ペットとのお出かけメディア-</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/ef0c77955e.js" crossorigin="anonymous"></script>
  </head>
  <body>
    <div class="hero-head">
      <header class="navbar">
        <div class="container">
          <div class="navbar-brand">
            <div class="header-search navbar-item is-fullwidth">
              <%= search_form_for @q, url: word_search_path, class: "header-search-form control has-icons-left" do |sf| %>
                <%= sf.fields_for :and, @q do |f| %>
                  <%= f.search_field :name_or_address_cont, placeholder: "施設の名前・住所を検索", class: "input" %>
                <% end %>
                <span class="icon is-small is-left">
                  <i class="fas fa-search"></i>
                </span>
              <% end %>
            </div>
            <h1 class="logo navbar-item">
              <%= link_to "Wan Family", root_path, title: "WanFamily[ワン・ファミリー] -ワンちゃん・ペットとのお出かけメディア-" %>
            </h1>
            <span class="navbar-burger burger" data-target="navbarMenuHeroC">
              <span></span>
              <span></span>
              <span></span>
            </span>
          </div>
          <div id="navbarMenuHeroC" class="navbar-menu">
            <div class="navbar-end">
              <%= link_to "Home", root_path, class: "navbar-item is-active" %>
              <%= render "top/shared/display_links/#{ who_signed_in? }_display", class_name: "navbar-item" %>
              <%= link_to "お出かけスポットを登録する", new_spot_path, "data-turbolinks": false, class: "navbar-item" %>
            </div>
          </div>
        </div>
      </header>
    </div>
    <section class="hero is-fullheight-with-navbar">
      <div class="hero-body is-flex-wrap-wrap">
        <%= yield %>
      </div>
    </section>
  </body>
</html>
