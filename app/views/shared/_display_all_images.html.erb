<% if blobs.present? %>
  <%= render "shared/display_sort_link", likes_count_title: "Cuteが多い順" %>
  <%= render "shared/display_image_list", blobs: blobs, review_image_only: false %>
<% else %>
  画像の投稿はありません。
<% end %>
