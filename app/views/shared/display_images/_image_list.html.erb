<section class="image-list-wrap">
  <% if image_blobs.present? %>
    <%= render "shared/display_images/image_link_list", image_blobs: image_blobs, spot_id: spot_id, image_id: image_id %>
  <% else %>
    画像の投稿はありません。
  <% end %>
</section>
<%= paginated?(image_blobs) %>
