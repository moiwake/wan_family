var favoritePost = document.getElementsByClassName("js-favorite-spot-post");
favoritePost[0].insertAdjacentHTML("afterend",
  "<%= j(
    render "favorite_spots/shared/add_or_remove_favorite_spot",
      spot: @spot,
      favorite_spot: @favorite_spot
  ) %>"
);
favoritePost[0].remove();
